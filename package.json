{"name": "clock_ext",
  "version": "1.0.2",
  "description": "In-browser timepiece.",
  "main": "manifest.json",
  "author": "AClark",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "web-ext -v run",
    "clean-build": "yarn clean && yarn build",
    "build": "tsc --project .",
    "clean": "rm -f js/*.js",
    "lint": "tslint -p ."
  },
  "dependencies": {
    "tslint": "^6.1.1"
  }
}
